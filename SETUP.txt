SILO — v0.2 DEPLOY NOTES (FLAT REPO)
====================================

FILES (all at repo root, no folders):
  index.html        Joe's app (SILO)
  sightings.json    registry data — edit this to add/move formations
  ax-k.html         hidden annex (search AX-K in-app)
  rec.html          withdrawn-records viewer (search a withdrawn ref in-app)
  gpi.html          ops console (agents + you)
  gpi.webmanifest   ops PWA identity ("Grain Price Index")
  gpi-192.png, gpi-512.png   fake icons
  sw.js             ops service worker — caches ONLY gpi files
  .nojekyll         required for Pages

1. REPO: new GitHub account, inert name (e.g. srv-archive-2209), public.
   Upload all files to root. Settings > Pages > Deploy from branch > main.

2. URLS
   Joe:      https://ACCOUNT.github.io/REPO/
   Ops:      https://ACCOUNT.github.io/REPO/gpi.html   <- paste into agent briefing CONFIG
   (ax-k.html and rec.html are reached in-app, never linked)

3. CODES (djb2-hashed in source)
   Operator FURROW-77 · Director (you) SCYTHE-19 · Decoy WINNOW-6
   Obfuscation, not security.

4. THE HIDDEN LAYER SO FAR
   - Manual auto-opens on Joe's first visit; section 7 lists WITHDRAWN as a
     status and says withdrawn refs "are still numbered" (the nudge).
   - ax-k.html tells him to find the missing numbers.
   - Searching VAD-0102 / 0105 / 0108 / 0112 / 0114 surfaces an ARCHIVED row
     -> rec.html dossier. Each dossier breadcrumbs a REAL googleable rabbit
     hole: Tully 1966 + ley alignments / San Luis Valley + Snippy /
     Rendlesham + the Halt memo / 4625 kHz (UVB-76) / Hessdalen.
   - Gaps 0103, 0106, 0109, 0117 remain unassigned = future episodes.

5. VERSION RULE: any change to gpi.html must bump its badge AND the CACHE
   name in sw.js (currently gpi-ops-v0.2) or installed phones won't update.

6. FIREBASE (phase 3): new project (not garrett-apps). Bring the config and
   I wire live sync + alert polling + rules.

7. PLANTED ANOMALY: VAD-0119 at 41.031, -81.207 (rural Portage Co. near
   Atwater). Adjust in sightings.json.
